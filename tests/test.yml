---
- hosts: localhost
  remote_user: root
  tasks:
  - include_role:
      name: shibboleth
    vars:
      applicationdefaults_entityid: 'https://SERVER.uu.se'
      sso_entityid: 'https://weblogin.uu.se/idp/shibboleth' 
      
      #roles:
          #- { role: shibboleth, applicationdefaults_entityid: 'https://SERVER.uu.se' }
    #- shibboleth
